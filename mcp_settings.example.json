{
  "mcpServers": {
    "ct_dev-agent_delegation": {
      "command": "python3",
      "args": ["-m", "ct_dev_agent_delegation_mcp.server"],
      "env": {
        "LOGFIRE_TOKEN": "${LOGFIRE_TOKEN}"
      }
    }
  }
}
